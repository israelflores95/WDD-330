document.forms // will call for all forms on the page
document.forms[0] // will call for the form by index 0 being the first and 1 the second...etc
document.forms[''] // will call for the form by name

const form = document.forms['search']

addform.add